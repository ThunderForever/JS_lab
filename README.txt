简易网页计算器使用方法
1.通过网页上的按键可以输入想要计算的表达式；
2.在网页计算器上的空白文本框处（1中算式显示的地方）同样可以输入，支持括号的使用，若是出错则不会改变文本显示，直至算式可以计算为止；
3.可以同时使用上述两种功能，即同时通过鼠标按键，又可以在文本框内修改；
4.支持多部运算，即上一次的运算结果可以直接使用；
5.只支持英语输入的括号，不支持其他语种输入的括号。

ps：关于单元测试部分，由于程序只使用了eval()函数，且程序复杂度不高，我认为可以不进行单元测试。


王力帆
516030910438
